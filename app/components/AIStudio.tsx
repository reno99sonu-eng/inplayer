// Unused — not rendered anywhere in the app (the real one is
// AIStudioModal.tsx). Emptied out rather than deleted: this session's tools
// can overwrite files on your machine but can't delete them. Safe to
// delete this file entirely whenever you get a chance.
export {};
